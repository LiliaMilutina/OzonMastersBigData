name: My Project

conda_env: conda.yml

entry_points:
  main:
    parameters:
      train_path: {type: string}
      model_param1: {type: float, default: 0.1}
    command: python train.py -- train_path={train_path} -- model_param1={model_param1}
